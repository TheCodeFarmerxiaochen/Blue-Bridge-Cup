#include <iostream>
#include <iomanip>
using namespace std;
int main()
{int n;
cin>>n;
cout<<endl;
int a[n];
for(int i=0;i<n;i++){
  cin>>a[i];
  cout<<endl;
}
int jige=0,youxiu=0;
for(int i=0;i<n;i++){
  if(a[i]>=60)
  jige+=1;
  if(a[i]>=85)
  youxiu+=1;
}
double c=100.0*jige/n;
double b=100.0*youxiu/n;
   cout<<setiosflags(ios::fixed)<<setprecision(0)<<c<<"%"<<endl;
   cout<<setiosflags(ios::fixed)<<setprecision(0)<<b<<"%"<<endl;
  return 0;
}
